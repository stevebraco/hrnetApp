const medias = {
  tablet: '@media (max-width: 768px)',
};

const colors = {
  bgBodyColor: '#171820',
  bgPrimaryColor: '#22232f',
  txtColor: '#ffffff',
};

export const theme = {
  colors,
  medias,
};
